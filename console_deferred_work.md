# Deferred work — console

1. 2026-04-09 | https://claude.ai/chat/e68ee425-14aa-40ae-8963-bd873d66f0f4 | new pipeline DAG stage | multi_entity_overlay stage post batch_manifest_intake. Calls Farm /generate-multi-entity-triples then push to canonical store (DCL vs Convergence TBC for customer.* triples). Own namespaced stage ID, fail-loud on Farm error, no silent skip on empty overlay JSONL. Option 1 chosen but not executed. Severity: blocker. Blocking: customer profile fields silently empty across all historical runs; cross-sell scoring stuck at 31/100 baseline. RESOLVED 2026-04-26 — Console rip-out of ME/MA. The multi_entity_overlay stage and all Convergence engines are now exclusively Convergence repo concerns. Console no longer orchestrates ME pipelines.
2. 2026-04-15 | brain-A-rename-session | frontend/e2e/engagement_move_g5.spec.ts:16 | G5 ME pipeline 6/6 e2e fails on dev (verified isolated checkout) — confirmed pre-existing baseline failure not caused by Brain-A rename | severity: degraded | blocking: full ME demo coverage in Playwright. RESOLVED 2026-04-26 — spec deleted (Console SE-only).
3. 2026-04-15 | brain-A-rename-session | frontend/e2e/engagement_move_g6.spec.ts:14 | G6 SE pipeline 6/6 e2e fails on dev (verified isolated checkout) — pre-existing baseline failure not caused by Brain-A rename | severity: degraded | blocking: full SE demo coverage in Playwright. RESOLVED 2026-04-26 — spec deleted (replaced by trimmed pipeline_e2e.spec.ts SE coverage).
4. 2026-04-15 | brain-A-rename-session | frontend/e2e/qofe.spec.ts:28 | QofE test targets `#nav-tab-reports` element that no longer exists in NLQ frontend (Reports tab removed/restructured); verified pre-existing on dev | severity: degraded | blocking: QofE Playwright coverage. RESOLVED 2026-04-26 — spec deleted (Convergence concern).
5. 2026-04-15 | brain-A-rename-session | frontend/e2e/verify_pipeline_complete.spec.ts:145 | ME verify-step success-prefix not appearing within timeout — depends on full pipeline data freshness; verified pre-existing on dev | severity: degraded | blocking: ME verify Playwright coverage. RESOLVED 2026-04-26 — ME helper + ME describe-block stripped; SE verify coverage retained.
6. 2026-04-15 | brain-A-rename-session | frontend/e2e/pipeline_e2e.spec.ts:263 | Order-dependent flake — passes in isolation, fails in full suite; B14 violation. Likely test-order state contamination upstream | severity: cosmetic | blocking: B14 determinism for full suite. RESOLVED 2026-04-26 — line 263 was an ME test; ME describe-block deleted.

7. 2026-04-15 | techflow-farm-400 session | frontend/src/pages/Pipeline.tsx:489 + backend/app/routes/engagements.py:44 + backend/app/services/convergence_client.py:47 + backend/app/services/pipeline_orchestrator.py:1675 | ME engagement dropdown fetched every engagement regardless of lifecycle_stage and auto-selected the newest, so a stale draft row hijacked the dropdown and its target_entity_id='techflow' propagated through preflight unchecked, causing Farm overlay 400 (pipeline_e2e.spec.ts:80). Severity: degraded. Blocking: ME e2e run ignores draft/paused/archived engagements and silently selects whichever one happens to be newest. RESOLVED 2026-04-15 (commit 094b707) — Pipeline.tsx now calls fetchConvergenceEngagements('active'); convergence_client.list_engagements forwards lifecycle_stage; routes/engagements.py accepts the query param; _me_preflight raises a loud RuntimeError if the fetched engagement's lifecycle_stage != 'active'. Three layers: dropdown filter + proxy passthrough + server-side guard.

8. 2026-04-15 | brain-BC-mai-v8 | backend/app/routes/mcp.py:35 | Console MCP `_SURFACE_STATE` is an in-memory dict keyed by session_id — works for single-process uvicorn dev only. When Console moves to multi-worker or horizontally scales, promote to Redis or shared Supabase row. Severity: degraded. Blocking: multi-worker Console MCP state sharing. RESOLVED 2026-04-16 mossy-inventing-lovelace — replaced in-memory dict with `console.surface_state_snapshots` Postgres table (PK session_id, payload JSONB, payload_hash, updated_at, 24h TTL sweep on read, hash-skip on identical writes). Frontend `useSurfaceState` adds 60s heartbeat re-using the existing publish path so an idle page recovers from a console-backend pm2 restart without operator interaction. Verified end-to-end: snapshot persists across restart, hash-skip preserves updated_at, Mai chat with restored snapshot answers "When was the last run?" with the correct AeroLabs run + timestamp. surface_state_e2e.spec.ts 11/11 pass.

9. 2026-04-15 | brain-BC-mai-v8 | backend/app/config.py:25 + backend/app/routes/identity.py | `AOS_OPERATOR_ID` env var + `/api/auth/identity` endpoint are single-operator dev stand-ins until real SSO ships. Frontend canonical envelope reads one operator_id from this surface. Severity: degraded. Blocking: multi-operator tenant support in Console.

10. 2026-04-15 | brain-BC-mai-v8 | frontend/e2e/mai_v8_perf_harness.spec.ts | 100-turn §11.5 performance harness written but not executed this session — requires Platform + Sonnet + full env up, and plan explicitly bars sample expansion. Run at Brain-BC exit gate with `MAI_PERF_RUN=1` before declaring §11.5 targets met. Severity: degraded. Blocking: Brain-BC exit gate v8.0 performance signoff.

11. 2026-04-15 | brain-BC-mai-v8 perf-harness-debug | see platform/app/mai/tools/registry.py (removed in this session) + dcl/backend/api/mcp_server.py:111 | Blueprint §5.3 specifies `query_triples` (domain-based triple retrieval, args: domains[]/entity_id/period/limit) but DCL's actual MCP tool is `query` (metric-based, args: metric/dimensions/grain). Removed `query_triples` from Mai registry to unblock harness. Proper fix is a real DCL `query_triples` handler that iterates domains and returns triples. Severity: degraded. Blocking: Mai direct-triple retrieval path for financial data questions.

12. 2026-04-15 | brain-BC-mai-v8 perf-harness-debug | see platform/app/mai/tools/registry.py (removed in this session) | `list_domains` tool was scope-creep in Brain-5 registry, not spec'd in blueprint §5 and not implemented in DCL MCP server. Removed to unblock harness. If Mai needs domain discovery, either add to DCL properly or have Mai use `semantic_export`. Severity: cosmetic. Blocking: nothing.

13. 2026-04-16 | mossy-inventing-lovelace surface-state | prod Console deployment | Localhost end-to-end verified (POST /api/mcp/surface-state then chat → Mai correctly identifies route, describes visible_panels + extras, no XML leakage in content stream). Prod-vs-local divergence ("/pipeline" reported on prod from /changes) is a deployment artifact: prod is running an older Console + Platform bundle that predates this session's surface-state push, registry contract fix, and dispatcher session_id injection. Required actions before prod will match local: redeploy Console frontend (SurfaceExtrasContext + useSurfaceState wired into 7 routes), redeploy Platform backend (registry input_schema fix + dispatcher.py auto-inject + CONSOLE_BASE_URL/CONSOLE_MCP_API_KEY in prod env). Severity: degraded. Blocking: prod parity with localhost for Mai's "what do you see?" path.

14. 2026-04-16 | mossy-inventing-lovelace surface-state | frontend/e2e/ (no spec yet) | No Playwright spec asserts get_surface_state correctness end-to-end (push from each route, ask Mai, verify route + visible_panels in tool_result). B11 says: if UI breaks and no test catches it, add a test. Add `surface_state_e2e.spec.ts` covering the 7 wired routes. Severity: degraded. Blocking: regression coverage on per-route extractors. RESOLVED 2026-04-16 — frontend/e2e/surface_state_e2e.spec.ts covers all 7 routes (per-route push + snapshot readback), /pipeline extras (step_statuses, recent_runs, pipeline_mode), and an end-to-end Mai chat that asserts `route: /pipeline` in the tool_result and rejects the "no snapshot pushed yet" note. 9/9 pass.

15. 2026-04-16 | concurrent-jumping-lobster diag | backend/app/services/pipeline_orchestrator.py:1687-1704 | `_me_preflight` silently defaults missing `acquirer_entity_id`/`target_entity_id` to empty string. RESOLVED 2026-04-26 — `_me_preflight` deleted entirely (Console SE-only).

16. 2026-04-16 | concurrent-jumping-lobster diag | backend/app/services/pipeline_orchestrator.py:1915 `run_single_step` | Step-by-step ME pipeline never calls `_me_preflight`. RESOLVED 2026-04-26 — ME branches deleted from `run_single_step` and `run_pipeline_batch`; `_me_preflight` deleted.

17. 2026-04-16 | mossy-inventing-lovelace surface-state | tests/test_pipeline.py::test_run_me_pipeline + ::test_me_pipeline_uses_convergence_engagement + ::test_cofa_sends_pipeline_run_id_to_convergence | All three fail with `lifecycle_stage=None`. RESOLVED 2026-04-26 — all three ME tests deleted (Console SE-only).

19. 2026-04-26 | console-se-only-rip | repo-wide | Console rip-out of ME/MA: top-bar Mode/Entity switchers deleted, EngagementContext + ChatScopeContext deleted, /api/engagements + convergence_client + ME pipeline orchestration paths deleted, PipelineMode enum + pipeline_mode column dropped from db.py CREATE TABLE. 16 frontend files + 7 e2e specs deleted. Backend tests 43/43 pass. Frontend `npm run build` clean. Pre-prod dev DB — existing pipeline_jobs/recon_history rows have a dangling pipeline_mode column nothing reads; harmless. Drop the table and let `CREATE TABLE IF NOT EXISTS` rebuild if a clean slate is wanted. Severity: cosmetic. Blocking: nothing.

18. 2026-04-18 | playwright-ui-driven-rule | frontend/e2e/surface_state_e2e.spec.ts:22,122,179 + frontend/e2e/qofe.spec.ts:18 | New `ui-bypass-fetch-call` pre-commit pattern (`.playwright-banned-patterns`) added this session per CLAUDE.md § Playwright Acceptance "UI-driven only" subsection. Console hook scans full file content (not added-lines-only like DCL), so any future commit touching these two files will be blocked until the `await fetch(...)` calls are replaced with operator clicks (or with `page.request.get()` for the `qofe.spec.ts:18` health-check read). surface_state_e2e.spec.ts MCP/Mai-chat fetches are the harder rewrite — they currently drive Mai through a backend POST instead of typing into the chat box. Severity: degraded. Blocking: any future edit to surface_state_e2e.spec.ts or qofe.spec.ts.

20. 2026-05-16 | WS-1 Block 8.7 | console/.env (gitignored) | Console's MCP-client path through `/api/pipelines/consumer/query` returned 502 with the message "DCL_MCP_TOKEN_SECRET is not set in Console env. Console MCP client cannot mint a tenant token without the shared secret." (correct A1 loud-fail). Pre-existing pipelines-consumer.spec.ts test failed in WS-1 Block 8.7 run #1. Root cause: Console's local `.env` lacked `DCL_MCP_TOKEN_SECRET`. DCL's `.env` has `DCL_MCP_TOKEN_SECRET=wp5-shim-secret-2026-rotate-when-platform-issuance-lands`. Local fix this session: appended that same secret to `/home/ilyac/code/console/.env`, restarted `console-backend` via pm2. Console-pipelines suite now 8/8 green (twice). The secret rotates when Platform issuance lands; until then, **every operator machine running Console must add `DCL_MCP_TOKEN_SECRET` to its local `console/.env`** — `.env` is gitignored so the fix does NOT propagate via commit. Severity: cosmetic (any operator's first run hits the loud A1 error and is told what to set). Blocking: pipelines-consumer.spec.ts on any fresh operator machine until the env var is set.

21. 2026-05-25 | deployment-tour-build | frontend/e2e/pipelines-catalog.spec.ts:25 + pipelines-identity.spec.ts:23 + pipelines-mappings.spec.ts:48 + pipeline_e2e.spec.ts (4 tests) + verify_pipeline_complete.spec.ts:33 | Pre-existing Playwright failures verified to predate this session via `git stash && retest`. (a) Catalog test fetches Console proxy `/api/pipelines/catalog` which returns 504 because upstream AAM has no `/api/aam/catalog` route — AAM exposes `/api/pipes` and `/api/aam/discovery/declared-pipes` instead, so Console's proxy URL is stale. (b) Identity + mappings tests fail for related AAM endpoint drift. (c) pipeline_e2e + verify_pipeline_complete tests wait for "Snapshot ready"/"Pipeline completed" main-region text that never appears — pipeline backend orchestration not producing the terminal status during e2e run. Console UI surfaces are not the issue. Fix scope spans Console backend proxy URLs + AAM endpoint surface (cross-repo); deferred from deployment-tour scope. Severity: degraded. Blocking: pipelines-catalog / pipelines-identity / pipelines-mappings / pipeline_e2e (4) / verify_pipeline_complete (1) suites until AAM endpoint contract + pipeline orchestration timing are reconciled.
    PARTIALLY RESOLVED 2026-06-10 (commit fef75d6) — (c) closed: pipeline_e2e.spec.ts + verify_pipeline_complete.spec.ts rewritten for the AOD-inclusive spine (Enterprise Snapshot mints the I2 pair → AOD Discovery → Validation Lab grade → AOD→AAM handoff → AAM inference → AAM records transport (one combined DCL ingest) → NLQ verify); orchestration reaches terminal status and both specs pass twice, 8/8 headless + 8/8 headed. (a) catalog proxy 504 and (b) identity/mappings AAM endpoint drift remain OPEN — cross-repo endpoint reconciliation unchanged.

22. 2026-05-25 | deployment-tour-build | frontend/e2e/surface_state_e2e.spec.ts:36 (/reports + /engagements push tests) + :65 (pipeline_mode in extras) + :91 (me_engagement_id scope leak) | Four surface_state_e2e tests target Console features removed in the SE-only rip (deferred #19): /reports + /engagements routes are gone, pipeline_mode column was dropped, me_engagement_id no longer exists in surface extras. Tests should have been deleted alongside the rip-out but were not. Cannot be edited in-place because deferred #18's `ui-bypass-fetch-call` pre-commit pattern blocks any commit touching surface_state_e2e.spec.ts until the inline `await fetch(...)` calls are rewritten to UI clicks — a substantial test rewrite. Recommended fix: delete the four obsolete tests AND rewrite the fetch() bypasses in the same commit. Out of scope for deployment-tour build. Severity: degraded. Blocking: surface_state_e2e suite (4 of 11 tests) until tests are pruned for SE-only Console + fetch() bypasses are replaced.

23. 2026-05-29 | claudemd-trim-propagation | aod/CLAUDE.md + farm/CLAUDE.md (Dev/Prod Database Separation section) | Trim+repoint+propagation pass converged all 11 repos onto the trimmed v8.6 base (architecture moved to governing docs; durable rules/guardrails retained). aod and farm originally lacked the `## Dev/Prod Database Separation` guardrail section; the wholesale base step added it to them for consistency (it is a guardrail, not architecture). Separately, durable-guardrail section-sets still differ across repos by design — aam carries a repo-specific `### DCL two-process model — DO NOT MODE-SWITCH` guardrail (preserved); only dcl carries `# Destructive-action contract (B19)` (preserved). Flag for review: either accept the aod/farm Dev/Prod DB addition, or revert those two repos to their original (no Dev/Prod DB), or normalize guardrail sections repo-wide in a separate pass. Severity: cosmetic. Blocking: nothing — CLAUDE.md is documentation; all repos unaffected at runtime.

24. 2026-06-20 | aod-pooler-fix-session | aam/.env.development DCL_URL=:8104 + aam/app/env_guard.py:25 | After fixing the AOD-Discovery pooler exhaustion (aod commit c713a49 — transaction-mode pooling), the SE pipeline now passes Enterprise Snapshot → AOD Discovery → Validation Lab → AOD→AAM Handoff but fails at AAM Transport → DCL: "DCLRecordsHandoffError ... POST http://localhost:8104/api/dcl/ingest-records?replace=true: ConnectError [Errno 111] Connection refused" across all 7 planes (erp/bi/ledger/ipaas/warehouse/event_bus/api_gateway). Root cause is a launch/port-scheme mismatch, NOT a code bug and NOT caused by the AOD fix (observed during diagnosis pre-fix): AAM's dev env_guard enforces DCL on :8104 (+ aos-dev glmeqbn), but no DCL listens on :8104 — DCL is running on :8004 (the opus port block per CLAUDE.md LOCAL DEVELOPMENT) and :8010. env_guard #42/#46 blocks simply repointing AAM dev to :8004 (treats :8004 as a prod marker), so this needs an Ilya decision: either launch a dev DCL on :8104, or reconcile the CLAUDE.md opus-block port (8004) with AAM's env_guard dev/prod port markers. Severity: blocker | blocking: full SE pipeline e2e completion (AAM Transport → DCL + Verify steps) and pipeline_e2e.spec.ts / verify_pipeline_complete.spec.ts going green, until a DCL instance is reachable on the dev port AAM expects. See aam_deferred_work.md if a canonical AAM entry is opened.
    RESOLVED 2026-06-20 — corrected: there is NO port mismatch and no env_guard conflict. The launch script (aos-launch.sh:53 + scripts/run_dcl_dev.sh) deliberately runs DCL as TWO processes — `dcl` :8004 = prod .env (Console health-probes it), `dcl-dev` :8104 = .env.development/aos-dev ("two processes, never the same"). AAM's :8104 target is correct. Root cause was simply that `dcl-dev` (:8104) was not running — absent from pm2; backends launch `--no-autorestart`, so once it died it stayed down. Fix (launch-script-faithful): started `dcl-dev-backend` via `pm2 start ... -- -c "exec bash scripts/run_dcl_dev.sh"` (:8104 healthy). Full SE pipeline then completed 6/6 green (run BlueFlow-344e-9a45: AAM Transport → DCL ingested 1,259 records / 7 planes / 209 concepts; Verify answered 3598.4 from DCL) and pipeline_e2e.spec.ts passed 6/6. No code/config change — AAM/env_guard were already correct.

25. 2026-06-24 | dcl-dev-durability + schema-consolidation session | aos-launch.sh:321 (dcl-dev autorestart) + backend/app/services/health_aggregator.py:14 + backend/app/config.py:24 (DCL_INGEST_BASE_URL) | dcl-dev (:8104) went silently dead AGAIN (recurrence of #24, now twice). Root cause of the recurrence: backends launch `--no-autorestart`, and Console only health-probed :8004 (prod-env DCL) so a dead :8104 read as "6/7 healthy" — the outage was invisible. RESOLVED 2026-06-24 at root: (a) aos-launch.sh starts dcl-dev with autorestart (`--max-restarts 15`) instead of `--no-autorestart`; (b) Console health now probes the real ingest DCL (:8104, label "DCL Ingest") in addition to :8004 — a dead :8104 now shows degraded. Local-only: aos-launch.sh + auto-derived `DCL_INGEST_BASE_URL` (only when `DCL_BASE_URL` contains :8004). Deployed unaffected — render `DCL_BASE_URL` is a port-less Render URL, so no extra probe is derived (radius-scanned). Verified: :8104 restart→ready, Console /api/health shows "DCL Ingest up", overall reflects it. Severity: was blocker (silent pipeline outage). Blocking: nothing now.
